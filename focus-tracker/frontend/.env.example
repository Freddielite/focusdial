# Only needed when the frontend is hosted separately from the backend
# (e.g. Vercel + Render). Leave commented out for local dev — Vite's dev
# server proxies /api to localhost:4000 instead (see vite.config.js).
# Uncomment and set this when deploying separately:
#VITE_API_URL=https://your-backend.onrender.com
