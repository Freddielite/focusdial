# Dependencies
node_modules/

# Env files — never commit real secrets (session secret, DB URL, VAPID
# keys, Google OAuth client secret, CRON_SECRET). Only .env.example
# should be tracked.
.env
.env.local
.env.*.local
backend/.env
frontend/.env

# Build output
frontend/dist/
frontend/build/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Editor / OS cruft
.DS_Store
.vscode/
.idea/

# Package manager
.npm/
